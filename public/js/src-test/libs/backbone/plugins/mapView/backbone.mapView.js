/*
 *  name: backbone.mapView,
 *  path: /backbone.mapView
 *  desc:
 */
define([
    'backbone',
    'leaflet'
], function (Backbone, L) {
    var MapView = Backbone.MapView = {};

    MapView

    return MapView;
});
